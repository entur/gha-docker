name: Entur/Meta/Doc
on:
  pull_request:

jobs:
  update-lint-doc:
    uses: entur/gha-meta/.github/workflows/auto-doc.yml@main
    with:
      workflowfile: .github/workflows/lint.yml
      readmefile: README-lint.md
